package com.company.chapter2;

public class Name {
    String first = "Theodore";      // write(setting it) to field
    String last = "Moose";          // write(setting it) to field
    String full = first + last;     // read(getting it) the fields first and last and then write(setting it) the field full
}
